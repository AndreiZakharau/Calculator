spring:
  datasource:
    url: jdbc:mysql://localhost:3306/calculator_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: nokia03111981
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show_sql: true
    format_sql: true
    hibernate.ddl-auto: update

server:
  port: 8082
  servlet:
    context-path: /api/v1.0